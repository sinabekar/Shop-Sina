// start loader for index.html

window.addEventListener("load", () => {
  let loader = document.querySelector(".loader");

  loader.classList.add("hidden");
  loader.className += "hidden";
  loader.className = "loader hidden";
});